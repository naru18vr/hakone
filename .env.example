# 任意: 経路プロバイダーのブラウザからアクセス可能なOSRM互換エンドポイント
# GitHub PagesではサーバーAPIを実行できないため、公開OSRMへ直接問い合わせます。
# 未設定時は公開OSRMデモサーバーを試行し、失敗時は直線の簡易ルートにフォールバックします。
NEXT_PUBLIC_ROUTING_API_URL=https://router.project-osrm.org

# 将来用: Google / Mapbox / OpenRouteService を接続するときは、必ずサーバー側で扱います。
# GOOGLE_MAPS_API_KEY=
# MAPBOX_ACCESS_TOKEN=
# OPENROUTESERVICE_API_KEY=
