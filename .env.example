# 任意: 経路プロバイダーのサーバー側エンドポイント（OSRM 互換）
# 未設定時は公開 OSRM デモサーバーを試行し、失敗時は直線の簡易ルートにフォールバックします。
ROUTING_API_URL=https://router.project-osrm.org

# 将来用: Google / Mapbox / OpenRouteService を接続するときは、必ずサーバー側で扱います。
# GOOGLE_MAPS_API_KEY=
# MAPBOX_ACCESS_TOKEN=
# OPENROUTESERVICE_API_KEY=
