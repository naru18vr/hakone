"use client";

import { useEffect, useMemo, useRef, useState } from "react";
import { CalendarPlus, MapPin, X } from "lucide-react";
import { hotelPoint } from "@/data/spots";
import { AddCustomRequest } from "@/lib/itinerary";
import { isOutsideHakoneArea, isValidCoordinates, locationFromSpot, roundCoordinate } from "@/lib/location";
import { CustomLocation, ItineraryItem, Spot } from "@/types";

type Kind = AddCustomRequest["type"];
type LocationMethod = "none" | "map" | "spot" | "odawara" | "hotel" | "selected" | "manual";
const defaults: Record<Kind, { label: string; title: string; minutes: number; description: string }> = {
  meal: { label: "食事", title: "昼食", minutes: 60, description: "昼食・カフェなどの滞在予定" }, break: { label: "休憩", title: "休憩", minutes: 20, description: "運転や歩行の合間の休憩" }, hotel: { label: "宿泊", title: "宿泊施設", minutes: 0, description: "この日の終了地点として扱います" }, rental_car: { label: "レンタカー", title: "レンタカー受取", minutes: 30, description: "受取・返却・給油などの手続き" }, transport: { label: "電車・交通", title: "小田原駅で乗車", minutes: 20, description: "乗車・乗換・徒歩などの移動予定" }, free: { label: "自由予定", title: "自由予定", minutes: 30, description: "買い物など自由に使う予定" }, travel_note: { label: "移動メモ", title: "移動", minutes: 0, description: "時刻とルートへ影響しないメモ" },
};
type Props = { day: 1 | 2; itinerary: ItineraryItem[]; spots: Spot[]; selectedSpot?: Spot; isLocationPicking: boolean; onStartMapPick: (commit: (location: CustomLocation) => void) => void; onAdd: (request: AddCustomRequest) => void; onClose: () => void };

export default function AddCustomItemDialog({ day: initialDay, itinerary, spots, selectedSpot, isLocationPicking, onStartMapPick, onAdd, onClose }: Props) {
  const closeRef = useRef<HTMLButtonElement>(null);
  const [kind, setKind] = useState<Kind>("meal"); const [title, setTitle] = useState(defaults.meal.title); const [day, setDay] = useState<1 | 2>(initialDay); const [minutes, setMinutes] = useState(60); const [placement, setPlacement] = useState<AddCustomRequest["placement"]>("end"); const [targetId, setTargetId] = useState(""); const [time, setTime] = useState(""); const [locationName, setLocationName] = useState(""); const [address, setAddress] = useState(""); const [note, setNote] = useState(""); const [reserved, setReserved] = useState(false); const [rentalAction, setRentalAction] = useState<NonNullable<AddCustomRequest["subtype"]>>("pickup"); const [transportMode, setTransportMode] = useState<NonNullable<AddCustomRequest["transportMode"]>>("train"); const [transportAction, setTransportAction] = useState<NonNullable<AddCustomRequest["transportAction"]>>("board"); const [departureTime, setDepartureTime] = useState(""); const [arrivalTime, setArrivalTime] = useState(""); const [destinationName, setDestinationName] = useState(""); const [locationMethod, setLocationMethod] = useState<LocationMethod>("none"); const [location, setLocation] = useState<CustomLocation>(); const [spotSearch, setSpotSearch] = useState(""); const [spotId, setSpotId] = useState(""); const [latitude, setLatitude] = useState(""); const [longitude, setLongitude] = useState(""); const [error, setError] = useState("");
  const items = itinerary.filter((item) => item.day === day).sort((a, b) => a.order - b.order);
  const matchingSpots = useMemo(() => spots.filter((spot) => spot.name.includes(spotSearch.trim()) || spot.category.includes(spotSearch.trim())).slice(0, 12), [spots, spotSearch]);
  const selectKind = (next: Kind) => { setKind(next); setTitle(defaults[next].title); setMinutes(defaults[next].minutes); setError(""); };
  const applyLocation = (next: CustomLocation | undefined, method: LocationMethod) => { setLocation(next); setLocationMethod(method); setLatitude(next ? String(next.latitude) : ""); setLongitude(next ? String(next.longitude) : ""); if (next?.name) setLocationName(next.name); };
  const applySpot = (id: string) => { setSpotId(id); const spot = spots.find((entry) => entry.id === id); if (spot) applyLocation(locationFromSpot(spot), "spot"); };
  useEffect(() => { closeRef.current?.focus(); }, []);
  useEffect(() => { if (!isLocationPicking) window.setTimeout(() => document.getElementById("custom-location-method")?.focus(), 0); }, [isLocationPicking]);
  useEffect(() => { const close = (event: KeyboardEvent) => { if (event.key === "Escape") onClose(); }; window.addEventListener("keydown", close); return () => window.removeEventListener("keydown", close); }, [onClose]);
  const submit = () => {
    if (!title.trim()) return setError("タイトルを入力してください"); if (!Number.isFinite(minutes) || minutes < 0 || minutes > 600) return setError("滞在時間は0分以上、600分以下で入力してください"); if ((kind === "meal" || kind === "break") && minutes === 0) return setError("食事・休憩は1分以上の滞在時間を入力してください");
    let finalLocation = location;
    if (locationMethod === "manual") { const lat = Number(latitude); const lng = Number(longitude); if (!isValidCoordinates(lat, lng)) return setError("緯度は-90以上90以下、経度は-180以上180以下で入力してください"); finalLocation = { name: locationName.trim() || undefined, latitude: roundCoordinate(lat), longitude: roundCoordinate(lng), source: "manual" }; }
    onAdd({ type: kind, title, day, stayMinutes: minutes, placement, targetId: targetId || undefined, requestedArrivalTime: time || undefined, locationName, address, note, isReserved: reserved, subtype: kind === "rental_car" ? rentalAction : undefined, transportMode: kind === "transport" ? transportMode : undefined, transportAction: kind === "transport" ? transportAction : undefined, departureTime: departureTime || undefined, arrivalTime: arrivalTime || undefined, destinationName: destinationName || undefined, location: finalLocation });
  };
  const targetNeeded = placement === "before" || placement === "after"; const isRental = kind === "rental_car"; const isTransport = kind === "transport"; const isHotel = kind === "hotel"; const isNote = kind === "travel_note"; const odawara = spots.find((spot) => spot.id === "odawara-station");
  if (isLocationPicking) return null;
  return <div className="dialog-backdrop" role="presentation" onMouseDown={(event) => { if (event.currentTarget === event.target) onClose(); }}><section className="add-dialog custom-dialog" role="dialog" aria-modal="true" aria-labelledby="custom-title"><div className="dialog-heading"><div><span className="eyebrow">予定を追加</span><h2 id="custom-title">旅行の予定を追加</h2></div><button ref={closeRef} className="icon-button" onClick={onClose} aria-label="予定追加を閉じる"><X size={20} /></button></div><div className="dialog-choice"><p>種類</p>{(Object.keys(defaults) as Kind[]).map((entry) => <button key={entry} className={kind === entry ? "active" : ""} onClick={() => selectKind(entry)}>{defaults[entry].label}</button>)}</div><p className="custom-kind-help">{defaults[kind].description}</p><div className="custom-fields"><label className="field-label">タイトル（必須）<input value={title} onChange={(event) => setTitle(event.target.value)} /></label><div className="scenario-grid"><label>日付<select value={day} onChange={(event) => { setDay(Number(event.target.value) as 1 | 2); setTargetId(""); }}><option value={1}>8月12日</option><option value={2}>8月13日</option></select></label><label>滞在時間（分）<input type="number" min="0" max="600" disabled={isHotel} value={minutes} onChange={(event) => setMinutes(Number(event.target.value))} />{isHotel && <small>宿泊は日程の終了として扱います</small>}</label></div>{isRental && <label className="field-label">手続き<select value={rentalAction} onChange={(event) => setRentalAction(event.target.value as typeof rentalAction)}><option value="pickup">受取</option><option value="return">返却</option><option value="procedure">手続き</option><option value="refuel">給油</option><option value="other">その他</option></select></label>}{isTransport && <><div className="scenario-grid"><label>移動手段<select value={transportMode} onChange={(event) => setTransportMode(event.target.value as typeof transportMode)}><option value="train">電車</option><option value="bus">バス</option><option value="walk">徒歩</option><option value="taxi">タクシー</option><option value="other">その他</option></select></label><label>内容<select value={transportAction} onChange={(event) => setTransportAction(event.target.value as typeof transportAction)}><option value="board">乗車</option><option value="exit">降車</option><option value="transfer">乗換</option><option value="move">移動</option></select></label></div><div className="scenario-grid"><label>出発予定<input type="time" value={departureTime} onChange={(event) => setDepartureTime(event.target.value)} /></label><label>到着予定<input type="time" value={arrivalTime} onChange={(event) => setArrivalTime(event.target.value)} /></label></div><label className="field-label">行き先（任意）<input value={destinationName} onChange={(event) => setDestinationName(event.target.value)} placeholder="例：東京駅" /></label></>}{!isNote && <label className="field-label">追加位置<select value={placement} onChange={(event) => { setPlacement(event.target.value as AddCustomRequest["placement"]); setTargetId(""); }}><option value="start">その日の最初</option><option value="end">その日の最後</option><option value="before">指定した予定の前</option><option value="after">指定した予定の後</option><option value="time">希望時刻に近い位置</option></select></label>}{targetNeeded && !isNote && <label className="field-label">基準予定<select value={targetId} onChange={(event) => setTargetId(event.target.value)}><option value="">選択してください</option>{items.map((item) => <option key={item.id} value={item.id}>{item.title}</option>)}</select></label>}{placement === "time" && !isNote && <label className="field-label">到着希望時刻<input type="time" value={time} onChange={(event) => setTime(event.target.value)} /></label>}<fieldset className="location-editor"><legend><MapPin size={14} /> 地図地点</legend><p>{location ? `設定済み：${location.name ?? `${location.latitude.toFixed(4)}, ${location.longitude.toFixed(4)}`}` : "地点なし：道路ルート計算の対象外です"}</p><label className="field-label">設定方法<select id="custom-location-method" value={locationMethod} onChange={(event) => { const method = event.target.value as LocationMethod; setLocationMethod(method); if (method === "none") applyLocation(undefined, method); }}><option value="none">地点を設定しない</option><option value="map">地図上で選択</option><option value="spot">既存スポットから選択</option><option value="odawara">小田原駅を使用</option><option value="hotel">宿泊地点を使用</option><option value="selected" disabled={!selectedSpot}>現在選択中の地点を使用</option><option value="manual">緯度・経度を手入力</option></select></label>{locationMethod === "map" && <button type="button" className="primary-button" onClick={() => onStartMapPick((next) => applyLocation(next, "map"))}>地図上で地点を選択</button>}{locationMethod === "spot" && <><label className="field-label">スポットを検索<input value={spotSearch} onChange={(event) => setSpotSearch(event.target.value)} placeholder="名称・カテゴリー" /></label><label className="field-label">既存スポット<select value={spotId} onChange={(event) => applySpot(event.target.value)}><option value="">選択してください</option>{matchingSpots.map((spot) => <option value={spot.id} key={spot.id}>{spot.name}（{spot.category}）</option>)}</select></label></>}{locationMethod === "odawara" && <button type="button" className="secondary-button" disabled={!odawara} onClick={() => odawara && applyLocation(locationFromSpot(odawara, "odawara"), "odawara")}>小田原駅を地点として使用</button>}{locationMethod === "hotel" && <button type="button" className="secondary-button" onClick={() => applyLocation({ name: hotelPoint.name, latitude: hotelPoint.latitude, longitude: hotelPoint.longitude, source: "hotel", spotId: hotelPoint.id }, "hotel")}>宿泊地点を地点として使用</button>}{locationMethod === "selected" && <button type="button" className="secondary-button" disabled={!selectedSpot} onClick={() => selectedSpot && applyLocation(locationFromSpot(selectedSpot, "selected"), "selected")}>現在選択中の地点を使用</button>}{locationMethod === "manual" && <div className="scenario-grid"><label>緯度<input inputMode="decimal" value={latitude} onChange={(event) => setLatitude(event.target.value)} placeholder="35.256200" /></label><label>経度<input inputMode="decimal" value={longitude} onChange={(event) => setLongitude(event.target.value)} placeholder="139.155000" /></label></div>}{location && <><small>緯度 {location.latitude.toFixed(6)} ／ 経度 {location.longitude.toFixed(6)}</small>{isOutsideHakoneArea(location) && <p className="location-warning">箱根周辺から大きく離れた地点です。入力内容をご確認ください。</p>}<button type="button" className="text-button danger" onClick={() => { if (window.confirm("地図地点を解除しますか？ この予定は道路ルート計算の対象外になります。")) applyLocation(undefined, "none"); }}>地図地点を解除</button></>}</fieldset><label className="field-label">場所名（任意）<input value={locationName} onChange={(event) => setLocationName(event.target.value)} placeholder="地図地点なし・ルート対象外" /></label><label className="field-label">住所（任意）<input value={address} onChange={(event) => setAddress(event.target.value)} /></label><label className="field-label">メモ（任意）<textarea value={note} onChange={(event) => setNote(event.target.value)} /></label><label className="share-note"><input type="checkbox" checked={reserved} onChange={(event) => setReserved(event.target.checked)} /> 予約済み</label></div>{error && <p className="field-error" role="alert">{error}</p>}<div className="dialog-actions"><button className="secondary-button" onClick={onClose}>キャンセル</button><button className="primary-button" disabled={targetNeeded && !targetId} onClick={submit}><CalendarPlus size={16} /> 予定を追加</button></div></section></div>;
}
